// Note that here I added only the methods that we frequently use!
export enum HttpMethods {
  GET = "get",
  POST = "post",
  PUT = "put",
  PATCH = "patch",
  DELETE = "delete",
}
export enum Roles {
  MANAGER = "MANAGER",
  ADMIN = "ADMIN",
  EMPLOYEE = "EMPLOYEE",
}
